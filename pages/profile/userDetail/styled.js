import ImageNext from '@/Components/ImageNext';
import styled from 'styled-components';

export const UserDetailContainer = styled.div`

`;

export const Avatar = styled(ImageNext)`
    
`;


export default () => {}
